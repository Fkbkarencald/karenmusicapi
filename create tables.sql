create table artist (
	artist_id int NOT NULL auto_increment,
    firstName varchar(255) NOT NULL,
    karenName varchar(255),
    showName varchar(255),
    LastName varchar(255),
    imageURL varchar(255),
    
    primary key(artist_id)
);

